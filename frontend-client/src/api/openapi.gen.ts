/**
 * Stub generated OpenAPI types — replaced by `npm run codegen` once the
 * backend is reachable at $VITE_API_BASE/openapi.json (defaults to
 * http://api.localhost:3000). The real file is committed-out via
 * .gitignore? — NO: it IS committed so CI builds work without the
 * backend running. Re-run codegen after every backend route change.
 *
 * The minimal `paths` / `components` shape below keeps `openapi-fetch`'s
 * generic constraints happy in the scaffold; real route signatures
 * appear after the first codegen run.
 */
export interface paths {}
export interface components {
  schemas: Record<string, never>;
}
export type webhooks = Record<string, never>;
export type operations = Record<string, never>;
export type $defs = Record<string, never>;
